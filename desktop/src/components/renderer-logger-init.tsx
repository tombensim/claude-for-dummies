"use client";

import "@/lib/renderer-logger";

// Client component that initializes the renderer logger.
// Renders nothing — just needs to be mounted once to run the side-effect import.
export default function RendererLoggerInit() {
  return null;
}
